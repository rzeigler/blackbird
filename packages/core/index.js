"use strict";

const {
    objOf,
    pipe
} = require("ramda");

const mkContext = (req) => {

}

const

const compile = (handler) => {
    return (req, res) => {

    }
};

module.exports = {
    compile
};
